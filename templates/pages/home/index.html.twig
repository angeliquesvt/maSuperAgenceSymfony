{% extends 'base.html.twig' %}

{% block body %}
    <div class="jumbotron text-center">
        <h1>Agence d'Angélique</h1>
        <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Libero tempore, necessitatibus facilis tenetur nemo distinctio. Reprehenderit iusto impedit a. Qui, quibusdam, ipsum quis facere sed provident. Nihil consequuntur soluta facilis.</p>
    </div>

    <div class="container">
        <h2>Les derniers biens</h2>
        <div class="row flex">
            {% for property in properties %}
                <div class="col-3">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title"><a href="{{ path("showproperty", { slug: property.slug, id: property.id }) }}">{{ property.title }}</a></h5>
                            <p class="card-text">{{ property.city }} ({{ property.postalCode }})</p>
                            <div class="text-primary" style="font-weight: bold; font-size:2rem;">{{property.formattedPrice}} €</div>
                        </div>
                    </div>
                </div>
            {% endfor %}

        </div>
    </div>
{% endblock %}
